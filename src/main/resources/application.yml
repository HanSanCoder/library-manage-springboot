
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql:///book?serverTimeZone=UTC
mybatis:
  mapper-locations: classpath:mapper/*.xml
#日志
logging:
  level:
    com:
      book:
        app:
          dao: debug
server:
  port: 8081
